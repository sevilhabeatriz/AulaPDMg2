import 'package:flutter/material.dart';

class Inicio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Início do Questionário'),
      ),
      body: Center(
        child: RaisedButton(
          onPressed: () {
            // Navegue para a próxima tela do questionário aqui
          },
          child: Text('Começar o Questionário'),
        ),
      ),
    );
  }
}
